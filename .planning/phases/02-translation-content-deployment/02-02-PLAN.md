---
phase: 02-translation-content-deployment
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [LANG-02, LANG-03, LANG-04, LANG-05, FORM-03, FORM-04]

must_haves:
  truths:
    - "All 8 English pages load at /en/ prefixed URLs with correct English content"
    - "All 8 Spanish pages load at /es/ prefixed URLs with correct Spanish content"
    - "Clicking internal CTAs on an EN page stays on /en/ URLs (no cross-language navigation)"
    - "Clicking internal CTAs on an ES page stays on /es/ URLs"
    - "English contact page at /en/contact/ shows form with English labels (Name, Email, Subject, Message, Send)"
    - "Spanish contact page at /es/contact/ shows form with Spanish labels (Nombre, Correo electrónico, Asunto, Mensaje, Enviar)"
    - "Division back-to-group button on EN pages links to /en/ (not /)"
    - "Division back-to-group button on ES pages links to /es/ (not /)"
  artifacts:
    - path: "/workspace/Vectanor-website/translate-pages.py"
      provides: "Script that was executed successfully"
      contains: "print.*Done"
  key_links:
    - from: "EN homepage CTA button"
      to: "/en/divisions/"
      via: "Elementor button widget link.url"
      pattern: "en/divisions"
    - from: "ES homepage CTA button"
      to: "/es/divisions/"
      via: "Elementor button widget link.url"
      pattern: "es/divisions"
    - from: "EN contact form _next"
      to: "https://vectanor.com/en/contact/?sent=1"
      via: "formsubmit.co hidden _next field"
      pattern: "_next.*en/contact"
    - from: "ES contact form _next"
      to: "https://vectanor.com/es/contact/?sent=1"
      via: "formsubmit.co hidden _next field"
      pattern: "_next.*es/contact"
---

<objective>
Run translate-pages.py to push all 16 translated pages (8 EN + 8 ES) to WordPress, then verify all pages render correctly with language-correct content and internal links.

Purpose: This is the deployment step — all translated content goes live. After this plan, EN and ES visitors can browse all 8 pages in their language with correct URLs and navigation.

Output: 16 live translated pages on vectanor.com with correct content, links, and contact forms.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-translation-content-deployment/02-01-SUMMARY.md
@.planning/phases/02-translation-content-deployment/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push all EN and ES pages via translate-pages.py</name>
  <files>/workspace/Vectanor-website/translate-pages.py</files>
  <action>
Verify translate-pages.py has no None values in the ID config (all IDs filled in from WP Admin stubs):

```bash
python3 -c "
import ast, sys
src = open('/workspace/Vectanor-website/translate-pages.py').read()
ast.parse(src)
# Check no None IDs remain
import re
nones = re.findall(r'(\"[a-z]+\")\s*:\s*None', src)
if nones:
    print('ERROR: Missing IDs for:', nones)
    sys.exit(1)
print('All IDs filled in')
"
```

If any None values remain, STOP and report which IDs are missing. Do NOT proceed until all IDs are filled in.

Once confirmed, run the script for EN first:
```bash
cd /workspace/Vectanor-website && python3 translate-pages.py en
```

Check output for each page — expect 7 lines like `-> [Title]: NNNN chars [publish]` for Elementor pages and `-> [Title]: raw HTML [publish]` for the contact page.

If EN succeeds, run ES:
```bash
cd /workspace/Vectanor-website && python3 translate-pages.py es
```

Expected: 8 EN pages pushed + 8 ES pages pushed (16 total).

**If a page push fails (HTTP error, auth error, ID not found):**
- Check the specific page ID exists: `curl -s "https://vectanor.com/wp-json/wp/v2/pages/[ID]" -u "dnoiseux:RcF2 FMi0 wjwu QlJV bNEo pPs5" | python3 -m json.tool | head -20`
- Verify the page is published and accessible
- Retry the specific page: `python3 translate-pages.py en vision` (if the script supports targeting)

Do NOT modify rebuild-wow.py under any circumstances. All changes happen only in translate-pages.py.
  </action>
  <verify>
    Run: `python3 translate-pages.py en && python3 translate-pages.py es`
    Expected: No errors, 16 total pages show [publish] status in output.
    Also verify EN homepage live: `curl -sI "https://vectanor.com/en/" | head -5` should return 200 OK.
    Verify ES homepage: `curl -sI "https://vectanor.com/es/" | head -5` should return 200 OK.
  </verify>
  <done>
    translate-pages.py ran successfully for both EN and ES with no errors. All 16 pages show [publish] status. curl checks for /en/ and /es/ return 200.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 16 translated pages render correctly</name>
  <what-built>All 16 translated pages pushed to WordPress: 8 English pages at /en/ URLs and 8 Spanish pages at /es/ URLs. Contact pages use raw HTML bypass with localized form labels. All internal links use language-prefixed URLs.</what-built>
  <how-to-verify>
    Check each URL in a browser. For each page: confirm the content is in the correct language, confirm internal navigation links work without switching to French.

    **English pages to check:**
    1. https://vectanor.com/en/ — Homepage in English. Hero tagline: "Sets the direction. Divisions advance. Systems follow." CTA button: "Discover our divisions" linking to /en/divisions/.
    2. https://vectanor.com/en/vision/ — "Our Vision" page in English. Section headings in English.
    3. https://vectanor.com/en/divisions/ — "Our Divisions" in English. Division cards have "Discover [Name] →" buttons linking to /en/divisions/[name]/.
    4. https://vectanor.com/en/divisions/dimonoff/ — Dimonoff page in English. "About Dimonoff" and "Areas of expertise" in English. "← Back to group" button links to /en/.
    5. https://vectanor.com/en/divisions/spatium/ — Spatium page in English.
    6. https://vectanor.com/en/divisions/amotus/ — Amotus page in English.
    7. https://vectanor.com/en/divisions/vigilia/ — Vigilia page in English.
    8. https://vectanor.com/en/contact/ — Contact form with English labels: Name, Email, Subject, Message, Send button. Subject dropdown: General Inquiry, Partnership, Media, Other.

    **Spanish pages to check:**
    9. https://vectanor.com/es/ — Homepage in Spanish. Hero tagline: "Marca la dirección. Las divisiones avanzan. Los sistemas siguen." CTA: "Descubrir nuestras divisiones" → /es/divisions/.
    10. https://vectanor.com/es/vision/ — "Nuestra Visión" in Spanish.
    11. https://vectanor.com/es/divisions/ — "Nuestras Divisiones" in Spanish. Cards → /es/divisions/[name]/.
    12. https://vectanor.com/es/divisions/dimonoff/ — In Spanish. "← Volver al grupo" → /es/.
    13. https://vectanor.com/es/divisions/spatium/ — In Spanish.
    14. https://vectanor.com/es/divisions/amotus/ — In Spanish.
    15. https://vectanor.com/es/divisions/vigilia/ — In Spanish.
    16. https://vectanor.com/es/contact/ — Spanish form: Nombre, Correo electrónico, Asunto, Mensaje, Enviar. Dropdown: Consulta general, Asociación, Medios, Otro.

    **Critical link checks:**
    - On an EN division page, click "← Back to group" — should land on /en/ (not /)
    - On the EN divisions index, click a "Discover [Name] →" button — should go to /en/divisions/[name]/
    - On any EN page, if you click the browser back button and navigate around, you should stay on /en/ URLs

    **If any page shows 404:** The page slug or parent assignment is wrong in WP Admin. Fix in WP Admin (check parent page assignment for division child pages).
    **If any page shows French content:** The page ID in translate-pages.py config pointed to a French page. Check the IDs.
    **If internal links cross to French:** The builder function still has unlocalized link URLs. Fix in translate-pages.py and re-run.

    Type "approved" if all 16 pages look correct, or describe which pages have issues.
  </how-to-verify>
  <resume-signal>Type "approved" if all pages are correct, or describe specific issues found (e.g., "EN dimonoff back button goes to / instead of /en/" or "ES contact form is blank").</resume-signal>
  <action>Human visits all 16 translated page URLs and verifies correct language content, correct internal links, and correct contact form labels (see how-to-verify for full checklist).</action>
  <verify>Human confirms: all 16 URLs load with correct language content, EN/ES internal links stay language-prefixed, contact forms show correct language labels.</verify>
  <done>Human types "approved" confirming all 16 pages render correctly in their respective languages with correct internal navigation.</done>
</task>

</tasks>

<verification>
- 16 pages pushed with no errors in script output
- curl /en/ and /es/ return 200 OK
- Human verified: all 16 pages load with correct language content
- Human verified: EN division pages back-to-group button links to /en/
- Human verified: ES division pages back-to-group button links to /es/
- Human verified: EN contact form has English labels
- Human verified: ES contact form has Spanish labels
</verification>

<success_criteria>
1. All 8 English pages exist at /en/ prefixed URLs with AI-translated English content (LANG-02, LANG-04)
2. All 8 Spanish pages exist at /es/ prefixed URLs with AI-translated Spanish content (LANG-03, LANG-04)
3. Internal links on /en/ pages point to /en/ URLs, internal links on /es/ pages point to /es/ URLs (LANG-05)
4. English contact page has form with English labels (FORM-03)
5. Spanish contact page has form with Spanish labels (FORM-04)
6. Human approval received for all 16 pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-translation-content-deployment/02-02-SUMMARY.md`
</output>
